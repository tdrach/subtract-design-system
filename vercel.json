{
  "installCommand": "npm install --include=dev && npm install --prefix preview --include=dev",
  "buildCommand": "npm run build --prefix preview",
  "outputDirectory": "preview/.next",
  "framework": "nextjs"
}
